# 83903 - Auto-title returns raw JSON fragments when max_tokens=64 truncates model response

- URL: https://github.com/NousResearch/hermes-agent/issues/83903
- Added: 2026-08-11

## Why claimable

P3 but high-value: 4 of last 10 sessions had garbage titles (`{"title"`,
raw JSON fragments) - visible user-facing degradation. Root cause named
precisely: agent/title_generator.py line 400 max_tokens=64 truncates the
two-stage title JSON mid-response; _extract_title_text (line 277) falls
through json.loads -> regex -> prose fallback which returns the raw
truncated fragment. Not provider-specific (any model ignoring
response_format or exceeding 64 tokens). Introduced in f726090d4
(2026-08-08) - recent regression.

## Status

- 2026-08-11: vetted, NOT claimed (session budget spent on 5 open PRs).
  Clean claim-check: no crossrefs, no open PR, no assignee, no comments.
  NEXT PICK for the next session. Fix surface: title_generator.py - the
  fallback must reject truncated/partial JSON (e.g. no closing brace/quote)
  and return a safe generic title instead of the raw fragment.
- 2026-08-11: claimed, fix built (structural rejection in _extract_title_text
  after the loose scan: unclosed fence or leading {/[ that failed to parse),
  39/39 tests incl. false-positive regressions, two-axis review consumed
  (ordering + bare-"title"-marker findings fixed). WITHDRAWN at PR time -
  competitor PR #83985 (olympusbuildz) covers the same class with a working
  fix. Posted review findings on #83985: its _is_title_fragment heuristic
  false-positives on markdown-emphasis/quoted prose; structural check avoids
  the collision; the Goal shape is unfixable by any approach.
