# 83847 - model_aliases: key_env silently ignored (custom provider forced onto OPENAI_API_KEY)

- URL: https://github.com/NousResearch/hermes-agent/issues/83847
- Added: 2026-08-11

## Why claimable

P2 config bug, root causes named precisely. (1) `DirectAlias` NamedTuple
(model_switch.py:363) carries no key field; `_load_direct_aliases` (377)
reads only model/provider/base_url, silently dropping `key_env` ->
runtime resolves `provider: custom` aliases via OPENAI_API_KEY ->
401. (2) aliases never appear in the model picker (second, bundled bug).

## Status

- 2026-08-11: vetted, NOT claimed. Fix surface is multi-part: DirectAlias +
  loader + alias-tuple contract `(provider, model, name)` unpacked at
  switch_model 1475/1516 + credential block, PLUS a second bundled bug
  (picker visibility). Shared-contract change with a bundled second issue -
  medium merge risk; parked as next pick rather than started half-way in a
  budget-limited session.
