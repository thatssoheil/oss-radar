# 83837 - model_switch.py section 3b hardcodes empty api_key when probing custom endpoints

- URL: https://github.com/NousResearch/hermes-agent/issues/83837
- Added: 2026-08-11

## Why claimable

P2 with the exact line named. Desktop model picker probing a bare custom
endpoint passed "" api_key -> authenticated endpoints 401 -> picker
collapsed to [current_model]. Chat unaffected (separate key path).

## Status

- 2026-08-11: claimed
- 2026-08-11: RCA - the issue's suggested fix is PARTIALLY WRONG: current_api_key kwarg is on switch_model (1292), NOT list_authenticated_providers (2056-2070); ConfigContext/load_picker_context carry no current_api_key. Fixed instead at the 3b call site: resolve model.api_key then key_env via _scoped_key_env from config, thread into the probe. No-key endpoints still probe (bare local endpoints need that).
- 2026-08-11: fix built (branch fix/model-picker-apikey) - 3/3 tests, RED verified (2 fail pre-fix), 18/18 with sibling suites.
- 2026-08-11: two-axis review (deleg_820b557f) - SPEC caught 2 gaps: model.api alias not honored + key_env precedence inverted vs runtime. Verified: runtime bare-custom path uses ("api_key", "api") loop with NO key_env (runtime_provider.py:1191-1195, 1271-1282) - dropped key_env fallback entirely, mirrored the loop exactly. STANDARDS: duplication (4th copy of resolution pattern), broad except, load_config vs readonly - all fixed (narrowed except, load_config_readonly).
- 2026-08-11: competing PR #83850 appeared mid-build (pass current_api_key) - its premise is WRONG: current_api_key not in scope in list_authenticated_providers (0 occurrences), would NameError silently swallowed, CI failing. Posted verified evidence on #83850, opened correct PR.
- 2026-08-11: PR open (#83930)
