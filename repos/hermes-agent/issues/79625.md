# 79625 - desktop checkpoints always disabled

- URL: https://github.com/NousResearch/hermes-agent/issues/79625
- Added: 2026-08-07

## Why claimable

`_make_agent` passed `checkpoints_enabled=is_truthy_value(os.environ.get("HERMES_TUI_CHECKPOINTS"))` - always False on desktop because the env var is never set. Real config was being ignored. Fix: `_resolve_checkpoints_enabled(cfg)`, env wins when set, else config section. Reviewer required wiring tests (drive `_make_agent` with capturing fake), not just helper tests.

## Status

- 2026-08-07: claimed - fix + wiring tests built, PR submitted
- 2026-08-07: PR open
